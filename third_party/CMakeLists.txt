cmake_minimum_required(VERSION 3.0.0)

project(third_party)

# NOTE: build with source
# SET DEBUG MODE

if(NOT CMAKE_BUILD_EXTERNAL_DEBUG)
set(CMAKE_BUILD_EXTERNAL_DEBUG "OFF")
endif()
message(STATUS "EXTERNAL LIBRARY OPTIONS")
message(STATUS "DEBUG MODE: ${CMAKE_BUILD_EXTERNAL_DEBUG}")
if (CMAKE_BUILD_EXTERNAL_DEBUG STREQUAL "ON")
set(CMAKE_BUILD_TYPE "Debug" CACHE STRING "Build Type" FORCE)
else()
set(CMAKE_BUILD_TYPE "Release" CACHE STRING "Build Type" FORCE)
endif()

# json_struct
# ADD_SUBDIRECTORY(json_struct)
#set(JSONSTRUCT_INCLUDE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/json_struct/include PARENT_SCOPE)
